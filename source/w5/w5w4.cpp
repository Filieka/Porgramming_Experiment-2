#include<iostream>
#include<vector>
using namespace std;

class RequestData{
private:
    int tag,num,start,needTime,frequency;
public:
    RequestData(int tag,int num,int start,int needTime,int frequency):tag(tag),num(num),start(start),needTime(needTime),frequency(frequency){}

};

int main(){
    int requestNum;
    cin>>requestNum;

}